import 'package:flutter/material.dart';

class semSelection extends StatelessWidget {
  const semSelection({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
